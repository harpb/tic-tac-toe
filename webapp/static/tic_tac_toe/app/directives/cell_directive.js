// Generated by CoffeeScript 1.8.0
(function() {
  var app;

  app = angular.module('app');

  app.directive('cell', function() {
    return {
      require: '^ticTacToe',
      scope: {
        model: '=ngModel'
      },
      templateUrl: 'app/directives/cell.html',
      controller: function($scope) {
        $scope.active = false;
        return $scope.letter = ' ';
      },
      link: function(scope, element, attrs, ticTacToeCtrl) {
        scope.activate = function() {
          if (scope.model.active) {
            return;
          }
          ticTacToeCtrl.playerMove(scope.model);
          return scope.model.active = true;
        };
        scope.rightCorner = function() {
          return this.model.position % ticTacToeCtrl.length === ticTacToeCtrl.length - 1;
        };
        return scope.color = function() {
          if (ticTacToeCtrl.isPlayer(this.model)) {
            return 'teal';
          }
          if (ticTacToeCtrl.isComputer(this.model)) {
            return 'purple';
          }
        };
      }
    };
  });

}).call(this);
