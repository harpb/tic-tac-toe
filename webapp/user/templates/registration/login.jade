| {% extends "base.html" %}

| {% block content %}
| {% if form.errors %}
.error.message.ui Your username and password didn't match. Please try again.
| {% endif %}
form.form.ui(
    method="post"
    action="{% url 'django.contrib.auth.views.login' %}"
)
    | {% csrf_token %}
    .field
        label {{ form.username.label_tag }}
        | {{ form.username }}
    .field
        label {{ form.password.label_tag }}
        | {{ form.password }}
    button.button.ui(type='submit') Login
| {% endblock%}